final class {{ cb.native.vtable_struct_name }} extends $$ffi.Struct {
  external $$ffi.Pointer<
    $$ffi.NativeFunction<
      $$ffi.Void Function($$ffi.Uint64)
    >
  > free;

  external $$ffi.Pointer<
    $$ffi.NativeFunction<
      $$ffi.Uint64 Function($$ffi.Uint64)
    >
  > clone;
{% for method in cb.native.methods %}
  external $$ffi.Pointer<
    $$ffi.NativeFunction<
      $$ffi.Void Function(
{%- for param in method.params %}
        {{ param.native_type.native_type() }},
{%- endfor %}
    )
    >
  > {{ method.vtable_field_name }};
{% endfor -%}
}

abstract interface class {{ cb.class_name }} {
{%- for method in cb.methods %}
  {% if method.is_async() %}Future<{{ method.ret_ty.dart_type() }}>{% else %}{{ method.ret_ty.dart_type() }}{% endif %} {{ method.name }}(
{%- for param in method.params %}
    {{ param.ty.dart_type() }} {{ param.name }},
{%- endfor %}
  );
{% endfor -%}
}

final class {{ cb.impl_class_name }} {
  static final _$$BoltFFIHandleMap<{{ cb.class_name }}> map = _$$BoltFFIHandleMap();

  static final _$$BoltFFIPtr vtable = (() {
    final vtable = _$$BoltFFICalloc.alloc(
      $$ffi.sizeOf<{{ cb.native.vtable_struct_name }}>(),
    );

    vtable.ptr.cast<{{ cb.native.vtable_struct_name }}>()
      ..ref.free = $$ffi.Pointer.fromFunction({{ cb.impl_class_name }}.free)
      ..ref.clone = $$ffi.Pointer.fromFunction({{ cb.impl_class_name }}.clone, 0)
{%- for method in cb.native.methods %}
      ..ref.{{ method.vtable_field_name }} = $$ffi.Pointer.fromFunction({{ cb.impl_class_name }}.{{ method.vtable_field_name }})
{%- endfor %};

    return vtable;
  })();

  static _$$BoltFFICallbackHandle createCallbackHandle({{ cb.class_name }} value) {
    return $$ffi.Struct.create()
      ..handle = map.insert(value)
      ..vtable = vtable.ptr;
  }

  static void free(int handle) => map.remove(handle);

  static int clone(int handle) => map.clone(handle);
{% for method in cb.native.methods %}
  static void {{ method.vtable_field_name }}(
{%- for param in method.params %}
    {{ param.native_type.dart_sub_type() }} {{ param.name }},
{%- endfor %}
  ){% if method.is_async() %} async{% endif %} {
    final l$impl = map.get(_p$handle);

    if (l$impl == null) {
      throw $$BoltFFIException(-1, "{{ cb.class_name }}: invalid handle `${_p$handle}`");
    }

    try {
    } catch (e) {
    }
  }
{% endfor %}
}
